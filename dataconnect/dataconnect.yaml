specVersion: "v1"
serviceId: "nlsa-app"
location: "us-central1"
schema:
  source: "./schema"
  datasource:
    postgresql:
      database: "fdcdb"
      cloudSql:
        instanceId: "nlsa-app-fdc"
      # schemaValidation: "COMPATIBLE"
connectorDirs: ["./connector"]
